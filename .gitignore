data-pipeline/.venv/
data-pipeline/.env
data-pipeline/output/
.claude/
__pycache__/

# Exclude credentials and GCP keys (any .json not explicitly allowed below)
*.json
!app/package.json
!app/package-lock.json
!app/tsconfig.json
!app/public/world-110m.geojson
!app/public/data/*.json
!vercel.json

# Keep parquet out of git (too large / binary; copy manually)
*.parquet
*.csv
